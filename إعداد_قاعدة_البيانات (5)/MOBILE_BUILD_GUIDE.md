# 📱 دليل بناء تطبيق NEW EGYPT GOLD للموبايل

## 🎯 نظرة عامة
هذا الدليل يشرح كيفية تحويل التطبيق إلى تطبيق موبايل لأندرويد و iOS باستخدام Capacitor.

---

## ✅ المتطلبات الأساسية

### لبناء تطبيق أندرويد:
- **Node.js** (مثبت بالفعل)
- **Android Studio** - [تحميل من هنا](https://developer.android.com/studio)
- **Java JDK 17** أو أحدث

### لبناء تطبيق iOS (Mac فقط):
- **Xcode** - [تحميل من App Store](https://apps.apple.com/us/app/xcode/id497799835)
- **CocoaPods** - تثبيت بالأمر: `sudo gem install cocoapods`

---

## 🚀 خطوات البناء

### الخطوة 1: تهيئة Capacitor
```bash
# تهيئة المشروع (مرة واحدة فقط)
npx cap init
```

عند السؤال:
- **App name**: NEW EGYPT GOLD
- **App ID**: com.newegyptgold.sales
- **Web directory**: dist

### الخطوة 2: بناء التطبيق
```bash
# بناء تطبيق الويب
npm run build
```

### الخطوة 3: إضافة المنصات

#### لأندرويد:
```bash
# إضافة منصة أندرويد
npx cap add android

# مزامنة الملفات
npx cap sync android

# فتح في Android Studio
npx cap open android
```

#### لـ iOS (Mac فقط):
```bash
# إضافة منصة iOS
npx cap add ios

# مزامنة الملفات
npx cap sync ios

# فتح في Xcode
npx cap open ios
```

---

## 📦 بناء ملف APK (أندرويد)

### من Android Studio:
1. افتح المشروع في Android Studio
2. انتظر حتى ينتهي Gradle من التحميل
3. اذهب إلى: **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
4. بعد الانتهاء، اضغط على **locate** لفتح مجلد الـ APK

### من سطر الأوامر:
```bash
cd android
./gradlew assembleDebug
# الملف سيكون في: android/app/build/outputs/apk/debug/app-debug.apk
```

---

## 🍎 بناء ملف IPA (iOS)

### من Xcode:
1. افتح المشروع في Xcode
2. اختر جهاز حقيقي أو محاكي
3. اذهب إلى: **Product** → **Archive**
4. بعد الانتهاء، اختر **Distribute App**
5. اتبع الخطوات لإنشاء ملف IPA

---

## 🔄 تحديث التطبيق بعد تعديل الكود

بعد أي تعديل على الكود:

```bash
# بناء ومزامنة
npm run build:mobile

# أو يدوياً:
npm run build
npx cap sync
```

ثم افتح Android Studio أو Xcode وشغل التطبيق.

---

## 🎨 تخصيص الأيقونة والشاشة الافتتاحية

### 1. إنشاء الأيقونة:
- حجم الأيقونة: **1024x1024 بكسل**
- صيغة: PNG بخلفية شفافة
- ضع الملف في: `resources/icon.png`

### 2. إنشاء شاشة البداية (Splash Screen):
- حجم: **2732x2732 بكسل**
- صيغة: PNG
- ضع الملف في: `resources/splash.png`

### 3. توليد الأحجام المختلفة:
```bash
# تثبيت أداة التوليد
npm install -g @capacitor/assets

# توليد جميع الأحجام
npx capacitor-assets generate
```

---

## 📱 اختبار التطبيق

### على محاكي أندرويد:
1. افتح Android Studio
2. اذهب إلى: **Tools** → **Device Manager**
3. أنشئ جهاز افتراضي جديد
4. شغل التطبيق من Android Studio

### على جهاز أندرويد حقيقي:
1. فعّل **وضع المطور** على الجهاز
2. فعّل **USB Debugging**
3. وصّل الجهاز بالكمبيوتر
4. شغل التطبيق من Android Studio

### على محاكي iOS:
1. افتح Xcode
2. اختر محاكي من القائمة العلوية
3. اضغط على زر التشغيل (▶️)

### على جهاز iPhone حقيقي:
1. وصّل الـ iPhone بالـ Mac
2. اختر الجهاز من Xcode
3. قد تحتاج حساب Apple Developer (مجاني للاختبار)

---

## 🚀 نشر التطبيق

### نشر على Google Play Store:

1. **إنشاء حساب مطور**:
   - اذهب إلى: [Google Play Console](https://play.google.com/console)
   - رسوم التسجيل: $25 (مرة واحدة)

2. **بناء نسخة الإنتاج**:
```bash
cd android
./gradlew bundleRelease
```

3. **توقيع التطبيق**:
   - أنشئ keystore جديد
   - وقّع ملف AAB
   - ارفعه إلى Play Console

4. **ملء معلومات التطبيق**:
   - الوصف
   - الصور
   - سياسة الخصوصية
   - الفئة

### نشر على App Store:

1. **إنشاء حساب مطور**:
   - اذهب إلى: [Apple Developer](https://developer.apple.com)
   - رسوم سنوية: $99

2. **إنشاء App ID**:
   - من Apple Developer Console
   - استخدم: com.newegyptgold.sales

3. **رفع التطبيق**:
   - من Xcode: **Product** → **Archive**
   - اختر **Distribute App**
   - اتبع الخطوات

4. **ملء معلومات التطبيق**:
   - من App Store Connect
   - أضف الوصف والصور
   - أرسل للمراجعة

---

## 🔧 حل المشاكل الشائعة

### مشكلة: Gradle Build Failed
**الحل**:
```bash
cd android
./gradlew clean
./gradlew build
```

### مشكلة: Pod Install Failed (iOS)
**الحل**:
```bash
cd ios/App
pod deintegrate
pod install
```

### مشكلة: التطبيق لا يتصل بـ Convex
**الحل**:
- تأكد من أن `VITE_CONVEX_URL` موجود في `.env.local`
- أعد بناء التطبيق: `npm run build:mobile`

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع [وثائق Capacitor](https://capacitorjs.com/docs)
2. راجع [وثائق Convex](https://docs.convex.dev)
3. تحقق من [مشاكل GitHub الشائعة](https://github.com/ionic-team/capacitor/issues)

---

## ✨ نصائح إضافية

### تحسين الأداء:
- استخدم صور مضغوطة
- قلل حجم الخطوط
- فعّل lazy loading للمكونات

### الأمان:
- لا تضع API keys في الكود
- استخدم environment variables
- فعّل ProGuard لأندرويد

### التحديثات:
- استخدم Capacitor Live Updates للتحديثات السريعة
- أو استخدم CodePush من Microsoft

---

## 🎉 تهانينا!

الآن لديك تطبيق موبايل كامل لـ NEW EGYPT GOLD! 🎊

يمكنك توزيعه على الموظفين أو نشره في المتاجر الرسمية.
