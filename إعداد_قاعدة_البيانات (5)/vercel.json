{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install"
}
git add vercel.json
git commit -m "Fix Vercel deployment with Convex backend"
git push

